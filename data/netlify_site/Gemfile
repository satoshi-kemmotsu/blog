source "https://rubygems.org"

# Netlify用のJekyll
gem "jekyll", "~> 4.3.0"

# Windows環境用（必要に応じて）
gem "wdm", "~> 0.1.1", :platforms => [:mingw, :x64_mingw, :mswin]

# Jekyll plugins
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-sitemap"
  gem "jekyll-seo-tag"
  gem "jekyll-paginate"
end